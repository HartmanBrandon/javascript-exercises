const removeFromArray = function(arr, removedItem) {

let returnedArr = [];
let index = arr.indexOf(removedItem);
return returnedArr.splice(index);

    // This is where I will remove from array
};

// Do not edit below this line
module.exports = removeFromArray;
